<style type="text/css">
table, th, td{
    text-align:center;
  border: 1px solid ;
}
</style>




<h1>ALL orders </h1>

<p> <%=link_to "new user", new_order_path %> </p>

<table>
    <thead>
        <tr>
        <td>id</td>
        <td>u</td>
        <td>p</td>
        <td></td>
        <td>change</td>
        <td>destro</td>
        <td>add product</td>
        <td>show product</td>
        </tr>
    </thead>
    <tbody>
    <% @orders.each do |table| %>
        <tr>
        <td> <%=table.id %> </td>
        <td> <%=table.user_id %> </td>
        <td> <%=table.product_id %> </td>
       
        <td>  </td>
        <td> <%=link_to "edit" ,edit_user_path(table.id) %> </td>
        <td> <%=link_to "delete", user_path(table.id), data: {turbo_method: :delete,turbo_confirm: "Are you sure?" } %> </td>
        <td> <%=link_to "add product", new_user_product_path(table.id) %> </td>
        <td> <%=link_to "show product", user_path(table.id) %> </td>
        <% end %>
        </tr>
    </tbody>